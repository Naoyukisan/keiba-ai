<% content_for :hide_navbar, true %>
<% content_for :title, "けいばAI｜トップ" %>

<div class="container py-5">
  <h1 class="display-5 fw-bold mb-3">けいばAI</h1>
  <p class="lead text-muted mb-4">
    レース条件を入れて「予想を始める」を押せば生成AIが予想します。<br>
    履歴閲覧や予想方法の改善もできます。
  </p>

  <% if user_signed_in? %>
    <div class="d-flex flex-column flex-sm-row gap-2">
      <%= link_to "予想ページへ", gemini_path, class: "btn btn-primary btn-lg" %>
      <%= link_to "履歴を見る", histories_path, class: "btn btn-outline-secondary btn-lg" %>
    </div>
  <% else %>
    <div class="d-flex flex-column flex-sm-row gap-2">
      <%= link_to "ログイン", new_user_session_path, class: "btn btn-primary btn-lg" %>
      <%= link_to "新規登録", new_user_registration_path, class: "btn btn-outline-secondary btn-lg" %>
      <%= link_to "管理者はこちら", new_admin_session_path, class: "btn btn-outline-secondary btn-lg" %>
    </div>

    <small class="text-muted d-block mt-2">アカウントが無い方は「新規登録」から。</small>
  <% end %>
</div>
