<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title><%= content_for(:title) || "エラー | けいばAI" %></title>

  <!-- 依存を減らすため、アプリのCSSは読み込まずCDNだけにするのが安全 -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
    crossorigin="anonymous">
  <style>
    body { min-height: 100vh; display: grid; place-items: center; background:#f8f9fa; }
    .card { max-width: 720px; }
    .mono { font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace; }
  </style>
</head>
<body>
  <main class="container py-5">
    <div class="card shadow-sm border-0 rounded-4 mx-auto">
      <div class="card-body p-4 p-md-5">
        <%= yield %>
        <div class="mt-4">
          <a href="<%= (defined?(unauthenticated_root_path) ? unauthenticated_root_path : "/") %>" class="btn btn-primary">
            トップへ戻る
          </a>
        </div>
      </div>
    </div>
  </main>
</body>
</html>
