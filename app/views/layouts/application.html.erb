<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="utf-8">
  <title><%= content_for(:title) || "けいばAI" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>

  <!-- Bootstrap CSS -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
    crossorigin="anonymous">

  <%= javascript_importmap_tags %>
</head>

<body>
  <nav class="navbar navbar-expand-md navbar-light bg-white shadow-sm px-3">
    <div class="container-fluid">
      <a class="navbar-brand fw-bold text-primary" href="<%= root_path %>">けいばAI</a>

      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMenu"
              aria-controls="navbarMenu" aria-expanded="false" aria-label="メニューを開く">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarMenu">
        <ul class="navbar-nav me-auto mb-2 mb-md-0">
          <li class="nav-item"><%= link_to "📊 予想", new_gemini_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to "📜 予想履歴", histories_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to "🔧 予想改善", new_improvement_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to " ✍予想方法", prediction_methods_path, class: "nav-link" %></li>
        </ul>

        <ul class="navbar-nav ms-auto mb-2 mb-md-0 align-items-center">
          <% if user_signed_in? %>
            <% if current_user.admin? %>
              <!-- ▼ 修正：ユーザー管理ボタン化 -->
              <li class="nav-item me-2">
                <%= link_to "ユーザー管理", admin_users_path, class: "btn btn-sm btn-outline-primary" %>
              </li>
              <!-- ▲ ここまで -->
            <% end %>

            <!-- ユーザーのメールアドレス表示 -->
            <li class="nav-item me-2 d-flex align-items-center">
              <span class="navbar-text small text-muted">
                <%= current_user.email %>
              </span>
            </li>

            <li class="nav-item">
              <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-sm btn-outline-secondary ms-2" %>
            </li>
          <% else %>
            <li class="nav-item me-2">
              <%= link_to "ログイン", new_user_session_path, class: "btn btn-sm btn-primary" %>
            </li>
            <li class="nav-item me-2">
              <%= button_to "ゲスト管理者でログイン", guest_admin_login_path, method: :post, class: "btn btn-sm btn-outline-danger" %>
            </li>
            <li class="nav-item">
              <%= button_to "ゲストユーザーでログイン", guest_user_login_path, method: :post, class: "btn btn-sm btn-outline-success" %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>

  <main class="container py-4">
    <%= render "shared/flash" rescue nil %>
    <%= yield %>
  </main>

  <!-- Bootstrap JS -->
  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"></script>
</body>
</html>
