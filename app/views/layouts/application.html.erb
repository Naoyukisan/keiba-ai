<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Keiba Ai" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

     <%= javascript_importmap_tags %>
     <!-- Bootstrap CSS -->
     <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Bootstrap JS (bundle: Popper込み) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>F
   </head>

  <body>
    <!-- モバイル用トップバー（md未満で表示） -->
    <nav class="navbar navbar-dark bg-dark d-md-none">
      <div class="container-fluid">
        <button class="btn btn-outline-light" type="button"
                data-bs-toggle="offcanvas" data-bs-target="#sidebar" aria-controls="sidebar">
          ☰ メニュー
        </button>
        <span class="navbar-brand ms-2">Keiba AI</span>
      </div>
    </nav>
  
    <div class="d-flex">
      <!-- サイドバー：md未満はOffcanvas、md以上は固定表示 -->
      <div id="sidebar"
           class="sidebar offcanvas-md offcanvas-start bg-dark text-white"
           tabindex="-1" data-bs-scroll="true" data-bs-backdrop="false" style="width: 250px;">
        <!-- Offcanvas ヘッダ（md未満で表示） -->
        <div class="offcanvas-header d-md-none">
          <h5 class="offcanvas-title">メニュー</h5>
          <button type="button" class="btn-close btn-close-white" data-bs-dismiss="offcanvas" aria-label="Close"></button>
        </div>
  
        <!-- 本体 -->
        <div class="offcanvas-body p-3">
          <h5 class="mb-3 d-none d-md-block">メニュー</h5>
          <ul class="nav nav-pills flex-column gap-1">
            <li class="nav-item">
              <a class="nav-link text-white" href="#">🏠 ホーム</a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-white" href="#">📊 予想</a>
            </li>
            <li class="nav-item">
              <a class="nav-link text-white" href="#">📁 マイページ</a>
            </li>
          </ul>
        </div>
      </div>
  
      <!-- 右側コンテンツ：領域がわかる簡易デザイン -->
      <main class="flex-grow-1 p-4 bg-body-tertiary border-start" style="min-height:100vh;">
        <div class="container-fluid">
          <h1 class="h4 mb-3">コンテンツ</h1>
  
          <div class="border rounded-3 p-4 bg-white">
            <p class="mb-2">ここに各ページの内容が表示されます。</p>
            <p class="text-muted small mb-4">（レイアウト確認用のダミーテキスト）</p>
  
            <div class="mt-2 p-5 border border-2 border-dashed rounded-3 text-center">
              ⇨ コンテンツ領域
            </div>
  
            <div class="mt-4">
              <%= yield %>
            </div>
          </div>
        </div>
      </main>
    </div>
  </body>
</html>
