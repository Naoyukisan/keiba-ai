<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "けいばAI" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  </head>

  <body class="min-vh-100">
    <!-- ★ 上部ナビゲーションバー -->
    <nav class="navbar navbar-expand-md navbar-dark bg-dark">
      <div class="container-fluid">
        <a class="navbar-brand fw-bold" href="#">けいばAI</a>

        <!-- ハンバーガーボタン（モバイル用） -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMenu"
                aria-controls="navbarMenu" aria-expanded="false" aria-label="メニューを開く">
          <span class="navbar-toggler-icon"></span>
        </button>

        <!-- ナビゲーションメニュー -->
        <div class="collapse navbar-collapse" id="navbarMenu">
          <ul class="navbar-nav ms-auto mb-2 mb-md-0">
            <li class="nav-item">
                <%= link_to "📊 予想", new_gemini_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "📜 予想履歴", histories_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">🔧 予想改善</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <!-- ★ コンテンツ本体 -->
    <main class="container py-4">
      <%= yield %>
    </main>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
